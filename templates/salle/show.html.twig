{% extends 'baseback.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
<div class="container-fluid contact bg-light py-5">
 <div class="container py-5">
    <h1>Salle</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Nom_salle</th>
                <td>{{ salle.nomSalle }}</td>
            </tr>
            <tr>
                <th>Description_salle</th>
                <td>{{ salle.descriptionSalle }}</td>
            </tr>
            <tr>
                <th>Region_salle</th>
                <td>{{ salle.regionSalle }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ salle.image }}</td>
            </tr>
            <tr>
                <th>Adresse_salle</th>
                <td>{{ salle.adresseSalle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_salle_edit', {'id': salle.id}) }}" class="primary-btn f-btn">edit</a>

    {{ include('salle/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
